# 🔥 抖音竞品分析 API 调用指南

**📅 生成时间**: 2026-02-28 19:40 (GMT+8)  
**📚 数据源**: api_by_tags 文件夹下的抖音相关 API 文件  
**🎯 技能**: SK-DATA-002 抖音竞品分析

---

## 📋 API 参数总览

| API 端点 | 必需参数 | 可选参数 | 状态 |
|---------|---------|---------|------|
| `/fetch_hot_account_list` | `date_window`, `page_num`, `page_size` | `query_tag` | ✅ |
| `/fetch_hot_account_item_analysis_list` | `sec_uid` | `day` | ✅ |

---

## 一、💬 热门账号

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_account_list
```

### 官方参数说明

**必需参数**:
- `date_window`: 时间窗口，格式 小时，默认 24 小时
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20

**可选参数**:
- `query_tag`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式
  ```json
  {"value": "{顶级垂类标签id}", "children": [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]}
  ```

### 调用示例

```bash
# 获取全部热门账号
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"date_window": 24, "page_num": 1, "page_size": 20}'

# 获取特定垂类热门账号
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"date_window": 24, "page_num": 1, "page_size": 20, "query_tag": {"value": "628", "children": [{"value": "62808"}]}}'
```

### Node.js 调用代码

```javascript
const axios = require('axios');

async function fetchHotAccountList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
const apiKey = 'YOUR_API_KEY'; // 请替换为您的 API 密钥
fetchHotAccountList({
  date_window: 24,
  page_num: 1,
  page_size: 20
}, apiKey).then(console.log);

// 获取特定垂类热门账号示例
fetchHotAccountList({
  date_window: 24,
  page_num: 1,
  page_size: 20,
  query_tag: {"value": "628", "children": [{"value": "62808"}]}
}, apiKey).then(console.log);
```

---

## 二、👥 账号作品分析

### API 端点
```
GET /api/v1/douyin/billboard/fetch_hot_account_item_analysis_list
```

### 官方参数说明

**必需参数**:
- `sec_uid`: 用户 sec_id

**可选参数**:
- `day`: 天数，默认 7 天

### 调用示例

```bash
# 获取账号作品分析（默认7天）
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_item_analysis_list?sec_uid=MS4wLjABAAAANXSltcLCzDGmdNFI2Q_QixVTr67NiYzjKOIP5s03CAE" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 获取账号作品分析（30天）
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_item_analysis_list?sec_uid=MS4wLjABAAAANXSltcLCzDGmdNFI2Q_QixVTr67NiYzjKOIP5s03CAE&day=30" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
async function fetchAccountItemAnalysis(sec_uid, day = 7, apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  const r = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_account_item_analysis_list', {
    headers,
    params: { sec_uid, day }
  });
  
  return r.data;
}

// 使用示例
fetchAccountItemAnalysis(
  'MS4wLjABAAAANXSltcLCzDGmdNFI2Q_QixVTr67NiYzjKOIP5s03CAE',
  7,
  apiKey
).then(console.log);

// 获取30天数据示例
fetchAccountItemAnalysis(
  'MS4wLjABAAAANXSltcLCzDGmdNFI2Q_QixVTr67NiYzjKOIP5s03CAE',
  30,
  apiKey
).then(console.log);
```

---

## 三、⚠️ 注意事项

1. **HTTP 方法**: 
   - 热门账号 API 使用 POST 方法
   - 账号作品分析 API 使用 GET 方法
   - 请严格按照 API 定义的方法进行调用

2. **参数格式**:
   - `query_tag` 参数需要以 JSON 格式传入
   - `sec_uid` 是用户的安全 ID，需要从用户信息 API 获取

3. **分页**:
   - 首次请求 `page_num=1`
   - 翻页时递增 page_num 值

4. **限流**:
   - 建议请求间隔≥1 秒
   - 批量获取时使用 Promise.all 并发

5. **API 密钥**:
   - 请将 `YOUR_API_KEY` 替换为您的实际 API 密钥
   - 密钥需要在 TikHub 平台获取

---

## 四、🔗 相关文档

- **API 文档**: https://api.tikhub.dev
- **Discord 支持**: https://discord.gg/aMEAS8Xsvz
- **充值页面**: https://user.tikhub.io/users/add_credit

---

**📧 文档生成**: 抖音数据获取策略组 - SK-DATA-002  
**🔧 技术支持**: TikTok Hub API Skill v1.0  
**📚 数据来源**: api_by_tags 文件夹下的抖音相关 API 文件