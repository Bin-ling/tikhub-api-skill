# 🔥 抖音内容创作辅助 API 调用指南

**📅 生成时间**: 2026-02-28 19:40 (GMT+8)  
**📚 数据源**: api_by_tags 文件夹下的抖音相关 API 文件  
**🎯 技能**: SK-DATA-006 抖音内容创作辅助

---

## 📋 API 参数总览

| API 端点 | 必需参数 | 可选参数 | 状态 |
|---------|---------|---------|------|
| `/fetch_hot_total_low_fan_list` | `page_num`, `page_size`, `date_window` | `tags` | ✅ |
| `/fetch_hot_total_high_play_list` | `page_num`, `page_size`, `date_window` | `tags` | ✅ |
| `/fetch_hot_total_high_like_list` | `page_num`, `page_size`, `date_window` | `tags` | ✅ |
| `/fetch_hot_total_high_fan_list` | `page_num`, `page_size`, `date_window` | `tags` | ✅ |

---

## 一、🚀 低粉爆款榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_low_fan_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `tags`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式
  ```json
  {"value": "{顶级垂类标签id}", "children": [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]}
  ```

### 调用示例

```bash
# 获取低粉爆款榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_low_fan_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'
```

### Node.js 调用代码

```javascript
const axios = require('axios');

async function fetchLowFanList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_low_fan_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
const apiKey = 'YOUR_API_KEY'; // 请替换为您的 API 密钥
fetchLowFanList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);
```

---

## 二、💡 高完播率榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_high_play_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `tags`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式
  ```json
  {"value": "{顶级垂类标签id}", "children": [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]}
  ```

### 调用示例

```bash
# 获取高完播率榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_play_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'
```

### Node.js 调用代码

```javascript
async function fetchHighPlayList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_play_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
fetchHighPlayList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);
```

---

## 三、👍 高点赞率榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_high_like_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `tags`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式
  ```json
  {"value": "{顶级垂类标签id}", "children": [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]}
  ```

### 调用示例

```bash
# 获取高点赞率榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_like_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'
```

### Node.js 调用代码

```javascript
async function fetchHighLikeList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_like_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
fetchHighLikeList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);
```

---

## 四、📈 高涨粉率榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_high_fan_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `tags`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式
  ```json
  {"value": "{顶级垂类标签id}", "children": [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]}
  ```

### 调用示例

```bash
# 获取高涨粉率榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_fan_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'
```

### Node.js 调用代码

```javascript
async function fetchHighFanList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_high_fan_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
fetchHighFanList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);
```

---

## 五、⚠️ 注意事项

1. **HTTP 方法**: 
   - 所有内容创作辅助相关 API 使用 POST 方法
   - 请严格按照 API 定义的方法进行调用

2. **参数说明**:
   - `tags` 参数需要以 JSON 格式传入
   - `date_window` 参数用于指定数据的时间窗口（按小时或按天）

3. **数据解读**:
   - 低粉爆款榜：发现粉丝量较低但播放量较高的内容
   - 高完播率榜：学习内容结构和节奏
   - 高点赞率榜：分析用户偏好和情感共鸣点
   - 高涨粉率榜：了解涨粉策略和内容形式

4. **限流**:
   - 建议请求间隔≥1 秒
   - 批量获取时使用 Promise.all 并发

5. **API 密钥**:
   - 请将 `YOUR_API_KEY` 替换为您的实际 API 密钥
   - 密钥需要在 TikHub 平台获取

---

## 六、🔗 相关文档

- **API 文档**: https://api.tikhub.dev
- **Discord 支持**: https://discord.gg/aMEAS8Xsvz
- **充值页面**: https://user.tikhub.io/users/add_credit

---

**📧 文档生成**: 抖音数据获取策略组 - SK-DATA-006  
**🔧 技术支持**: TikTok Hub API Skill v1.0  
**📚 数据来源**: api_by_tags 文件夹下的抖音相关 API 文件