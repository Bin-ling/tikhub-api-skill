# 🔥 抖音内容效果评估 API 调用指南

**📅 生成时间**: 2026-02-28 19:40 (GMT+8)  
**📚 数据源**: api_by_tags 文件夹下的抖音相关 API 文件  
**🎯 技能**: SK-DATA-003 抖音内容效果评估

---

## 📋 API 参数总览

| API 端点 | 必需参数 | 可选参数 | 状态 |
|---------|---------|---------|------|
| `/fetch_hot_item_trends_list` | `aweme_id`, `option`, `date_window` | - | ✅ |
| `/fetch_hot_comment_word_list` | `aweme_id` | - | ✅ |

---

## 一、📈 作品数据趋势

### API 端点
```
GET /api/v1/douyin/billboard/fetch_hot_item_trends_list
```

### 官方参数说明

**必需参数**:
- `aweme_id`: 作品 id
- `option`: 选项
  - `7` - 点赞量
  - `8` - 分享量
  - `9` - 评论量
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

### 调用示例

```bash
# 获取作品点赞量趋势（按小时）
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_item_trends_list?aweme_id=7372484719365098803&option=7&date_window=1" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 获取作品评论量趋势（按天）
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_item_trends_list?aweme_id=7372484719365098803&option=9&date_window=2" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
const axios = require('axios');

async function fetchItemTrends(aweme_id, option, date_window, apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  const r = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_item_trends_list', {
    headers,
    params: { aweme_id, option, date_window }
  });
  
  return r.data;
}

// 使用示例
const apiKey = 'YOUR_API_KEY'; // 请替换为您的 API 密钥

// 获取点赞量趋势（按小时）
fetchItemTrends(
  '7372484719365098803',
  7,
  1,
  apiKey
).then(console.log);

// 获取评论量趋势（按天）
fetchItemTrends(
  '7372484719365098803',
  9,
  2,
  apiKey
).then(console.log);
```

---

## 二、💭 作品评论分析

### API 端点
```
GET /api/v1/douyin/billboard/fetch_hot_comment_word_list
```

### 官方参数说明

**必需参数**:
- `aweme_id`: 作品 id

### 调用示例

```bash
# 获取作品评论分析
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_comment_word_list?aweme_id=7372484719365098803" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
async function fetchCommentWordList(aweme_id, apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  const r = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_comment_word_list', {
    headers,
    params: { aweme_id }
  });
  
  return r.data;
}

// 使用示例
fetchCommentWordList('7372484719365098803', apiKey).then(console.log);
```

---

## 三、⚠️ 注意事项

1. **HTTP 方法**: 
   - 所有内容效果评估相关 API 使用 GET 方法
   - 请严格按照 API 定义的方法进行调用

2. **参数说明**:
   - `aweme_id` 是作品的唯一标识，需要从作品链接或其他 API 获取
   - `option` 参数用于指定要获取的数据类型（点赞、分享或评论）
   - `date_window` 参数用于指定数据的时间窗口（按小时或按天）

3. **数据解读**:
   - 作品数据趋势 API 返回指定时间段内的数据变化趋势
   - 作品评论分析 API 返回评论词云权重，可用于分析用户反馈

4. **限流**:
   - 建议请求间隔≥1 秒
   - 批量获取时使用 Promise.all 并发

5. **API 密钥**:
   - 请将 `YOUR_API_KEY` 替换为您的实际 API 密钥
   - 密钥需要在 TikHub 平台获取

---

## 四、🔗 相关文档

- **API 文档**: https://api.tikhub.dev
- **Discord 支持**: https://discord.gg/aMEAS8Xsvz
- **充值页面**: https://user.tikhub.io/users/add_credit

---

**📧 文档生成**: 抖音数据获取策略组 - SK-DATA-003  
**🔧 技术支持**: TikTok Hub API Skill v1.0  
**📚 数据来源**: api_by_tags 文件夹下的抖音相关 API 文件