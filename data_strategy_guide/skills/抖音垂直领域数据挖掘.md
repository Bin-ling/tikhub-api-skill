# 🔥 抖音垂直领域数据挖掘 API 调用指南

**📅 生成时间**: 2026-02-28 19:40 (GMT+8)  
**📚 数据源**: api_by_tags 文件夹下的抖音相关 API 文件  
**🎯 技能**: SK-DATA-005 抖音垂直领域数据挖掘

---

## 📋 API 参数总览

| API 端点 | 必需参数 | 可选参数 | 状态 |
|---------|---------|---------|------|
| `/fetch_content_tag` | - | - | ✅ |
| `/fetch_hot_total_topic_list` | `page_num`, `page_size`, `date_window` | `tags` | ✅ |

---

## 一、🔍 垂类内容标签

### API 端点
```
GET /api/v1/douyin/billboard/fetch_content_tag
```

### 官方参数说明

**必需参数**:
- 无

### 调用示例

```bash
# 获取垂类内容标签
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_content_tag" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
const axios = require('axios');

async function fetchContentTag(apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  const r = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_content_tag', {
    headers
  });
  
  return r.data;
}

// 使用示例
const apiKey = 'YOUR_API_KEY'; // 请替换为您的 API 密钥
fetchContentTag(apiKey).then(console.log);
```

---

## 二、📊 话题热榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_topic_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `tags`: 子级垂类标签，空则为全部，多个标签需传入 JSON 格式的列表
  ```json
  [
    {"value": "{子级垂类标签id}"},
    {"value": "{子级垂类标签id}"}
  ]
  ```

### 调用示例

```bash
# 获取话题热榜（按小时）
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_topic_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'

# 获取特定垂类话题热榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_topic_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1, "tags": [{"value": "62808"}]}'
```

### Node.js 调用代码

```javascript
async function fetchTopicList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_topic_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
fetchTopicList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);

// 获取特定垂类话题热榜示例
fetchTopicList({
  page_num: 1,
  page_size: 20,
  date_window: 1,
  tags: [{"value": "62808"}]
}, apiKey).then(console.log);
```

---

## 三、⚠️ 注意事项

1. **HTTP 方法**: 
   - 垂类内容标签 API 使用 GET 方法
   - 话题热榜 API 使用 POST 方法
   - 请严格按照 API 定义的方法进行调用

2. **参数说明**:
   - `tags` 参数需要以 JSON 格式传入
   - 垂类内容标签 API 无需参数，直接调用即可

3. **数据解读**:
   - 垂类内容标签 API 返回完整的垂类标签体系
   - 话题热榜 API 返回特定垂类的热门话题

4. **限流**:
   - 建议请求间隔≥1 秒
   - 批量获取时使用 Promise.all 并发

5. **API 密钥**:
   - 请将 `YOUR_API_KEY` 替换为您的实际 API 密钥
   - 密钥需要在 TikHub 平台获取

---

## 四、🔗 相关文档

- **API 文档**: https://api.tikhub.dev
- **Discord 支持**: https://discord.gg/aMEAS8Xsvz
- **充值页面**: https://user.tikhub.io/users/add_credit

---

**📧 文档生成**: 抖音数据获取策略组 - SK-DATA-005  
**🔧 技术支持**: TikTok Hub API Skill v1.0  
**📚 数据来源**: api_by_tags 文件夹下的抖音相关 API 文件