# 🔥 抖音热点追踪与分析 API 调用指南

**📅 生成时间**: 2026-02-28 19:40 (GMT+8)  
**📚 数据源**: api_by_tags 文件夹下的抖音相关 API 文件  
**🎯 技能**: SK-DATA-001 抖音热点追踪与分析

---

## 📋 API 参数总览

| API 端点 | 必需参数 | 可选参数 | 状态 |
|---------|---------|---------|------|
| `/fetch_hot_total_list` | `type`, `page_num`, `page_size` | `snapshot_time`, `sentence_tag` | ✅ |
| `/fetch_hot_rise_list` | `page_num`, `page_size` | `order`, `sentence_tag` | ✅ |
| `/fetch_hot_total_search_list` | `page_num`, `page_size`, `date_window` | `keyword` | ✅ |

---

## 一、📊 热点总榜

### API 端点
```
GET /api/v1/douyin/billboard/fetch_hot_total_list
```

### 官方参数说明

**必需参数**:
- `type`: 快照类型
  - `snapshot` - 按时刻查看
  - `range` - 按时间范围

**条件参数**:
- `snapshot_time`: 快照时间 (当 type=snapshot 时必需)
  - 格式：`yyyyMMddHHmmss`
  - 示例：`20260228194000`
- `start_date`: 快照开始时间 (当 type=range 时必需)
  - 格式：`yyyyMMdd`
  - 示例：`20260228`
- `end_date`: 快照结束时间 (当 type=range 时必需)
  - 格式：`yyyyMMdd`
  - 示例：`20260228`

**可选参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `sentence_tag`: 热点分类标签，从热点榜分类获取，多个分类用逗号分隔，空为全部
- `keyword`: 热点搜索词

### 调用示例

```bash
# 方式 1: 按时刻查看 (snapshot)
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_list?type=snapshot&snapshot_time=20260228194000&page_num=1&page_size=20" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 方式 2: 按时间范围查看 (range)
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_list?type=range&start_date=20260228&end_date=20260228&page_num=1&page_size=20" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
const axios = require('axios');

async function fetchHotTotalList(type, params, apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  // 方式 1: snapshot
  const r1 = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_list', {
    headers,
    params: { type, ...params }
  });
  
  // 方式 2: range
  const r2 = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_list', {
    headers,
    params: { type, ...params }
  });
  
  return r1.data;
}

// 使用示例
const apiKey = 'YOUR_API_KEY'; // 请替换为您的 API 密钥
fetchHotTotalList('snapshot', {
  snapshot_time: '20260228194000',
  page_num: 1,
  page_size: 20
}, apiKey).then(console.log);
```

---

## 二、🏆 上升热点榜

### API 端点
```
GET /api/v1/douyin/billboard/fetch_hot_rise_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20

**可选参数**:
- `order`: 排序方式
  - `rank` - 按热度排序
  - `rank_diff` - 按排名变化
- `sentence_tag`: 热点分类标签，从热点榜分类获取，多个分类用逗号分隔，空为全部
- `keyword`: 热点搜索词

### 调用示例

```bash
# 按热度排序
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_rise_list?page_num=1&page_size=20&order=rank" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 按排名变化排序
curl -X GET "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_rise_list?page_num=1&page_size=20&order=rank_diff" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

### Node.js 调用代码

```javascript
async function fetchHotRiseList(params, apiKey) {
  const headers = { 'Authorization': 'Bearer ' + apiKey };
  
  const r = await axios.get('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_rise_list', {
    headers,
    params
  });
  
  return r.data;
}

// 使用示例
fetchHotRiseList({
  page_num: 1,
  page_size: 20,
  order: 'rank'
}, apiKey).then(console.log);
```

---

## 三、🎬 搜索热榜

### API 端点
```
POST /api/v1/douyin/billboard/fetch_hot_total_search_list
```

### 官方参数说明

**必需参数**:
- `page_num`: 页码，默认 1
- `page_size`: 每页数量，默认 20
- `date_window`: 时间窗口
  - `1` - 按小时
  - `2` - 按天

**可选参数**:
- `keyword`: 搜索关键字

### 调用示例

```bash
# 按小时查看搜索热榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_search_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 1}'

# 按天查看搜索热榜
curl -X POST "https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_search_list" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"page_num": 1, "page_size": 20, "date_window": 2}'
```

### Node.js 调用代码

```javascript
async function fetchHotSearchList(params, apiKey) {
  const headers = { 
    'Authorization': 'Bearer ' + apiKey,
    'Content-Type': 'application/json'
  };
  
  const r = await axios.post('https://api.tikhub.dev/api/v1/douyin/billboard/fetch_hot_total_search_list', 
    params, {
      headers
    }
  );
  
  return r.data;
}

// 使用示例
fetchHotSearchList({
  page_num: 1,
  page_size: 20,
  date_window: 1
}, apiKey).then(console.log);
```

---

## 四、⚠️ 注意事项

1. **HTTP 方法**: 
   - 所有热点追踪相关 API 使用 GET 方法，除了搜索热榜使用 POST 方法
   - 请严格按照 API 定义的方法进行调用

2. **时间格式**:
   - `snapshot_time`: `yyyyMMddHHmmss` (如：20260228194000)
   - `start_date/end_date`: `yyyyMMdd` (如：20260228)

3. **参数依赖**:
   - `type=snapshot` 时必须提供 `snapshot_time`
   - `type=range` 时必须提供 `start_date` 和 `end_date`

4. **分页**:
   - 首次请求 `page_num=1`
   - 翻页时递增 page_num 值

5. **限流**:
   - 建议请求间隔≥1 秒
   - 批量获取时使用 Promise.all 并发

6. **API 密钥**:
   - 请将 `YOUR_API_KEY` 替换为您的实际 API 密钥
   - 密钥需要在 TikHub 平台获取

---

## 五、🔗 相关文档

- **API 文档**: https://api.tikhub.dev
- **Discord 支持**: https://discord.gg/aMEAS8Xsvz
- **充值页面**: https://user.tikhub.io/users/add_credit

---

**📧 文档生成**: 抖音数据获取策略组 - SK-DATA-001  
**🔧 技术支持**: TikTok Hub API Skill v1.0  
**📚 数据来源**: api_by_tags 文件夹下的抖音相关 API 文件